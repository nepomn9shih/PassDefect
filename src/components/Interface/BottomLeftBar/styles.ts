import styled from 'styled-components';

export const StyledBottomLeftBar = styled.div`
    position: fixed;
    bottom: 0;
    left: 0;
    margin: 20px;
    display: flex;
    align-items: center;
    gap: 16px;
    box-sizing: border-box;
`;
